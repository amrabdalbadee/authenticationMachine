# Egyptian ID Extractor – Requirements
# Install with: pip install -r requirements.txt

# ── Core (always needed) ──────────────────────────────────────────────────────
pillow>=10.0.0            # Image loading & preprocessing
requests>=2.31.0          # HTTP for Ollama backend

# ── Backend: Qwen2-VL-2B (recommended, best Arabic OCR) ──────────────────────
# ~3.5GB RAM on CPU, supports Arabic natively
transformers>=4.45.0
torch>=2.2.0              # CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
qwen-vl-utils>=0.0.8      # Qwen2-VL image preprocessing
accelerate>=0.30.0        # Model loading helpers

# ── Backend: moondream2 (lightest, ~2GB RAM) ──────────────────────────────────
einops>=0.7.0             # Required by moondream

# ── Backend: Claude API (cloud fallback) ──────────────────────────────────────
anthropic>=0.34.0         # pip install anthropic

# ── Optional: GPU acceleration (uncomment if CUDA available) ──────────────────
# torch>=2.2.0+cu121      # CUDA version — much faster inference
# bitsandbytes>=0.43.0    # 4-bit quantization (CUDA only) → reduces to ~1.2GB VRAM
